clear
close all
clc
%--------------primer orden-------------
k=1;
tao=4;
spo=tf(k,[tao 1]);
subplot(3,1,1)
step(spo)

za = tf([tao 1],1);
subplot(3,1,2)
step(spo*za)

tao = 1;
pa = tf(1,[tao 1]);
subplot(3,1,3)
step(spo*pa)

%--------------segundo orden-------------
e=2;
wn=2;
k=1;
sso=tf(k*wn^2,[1 2*e*wn wn^2]); 
figure
step(sso)

